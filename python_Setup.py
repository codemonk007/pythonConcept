python comes with default pip bundle.
To set up python environment we have to install virtual environment to safegaurd version dependancy.
pip version freeze - freeze python version 
pip install vertualenv - to install vertual environment
vertualenv venv --python=python3.5


then source it 
source venv/bin/activate


for flask 
python --version
pip install flast-restful
* import Flast,Resource,APi
class item{

    def get(self,name)
}
addresource(item,'/tere')

# use pass for no implementation 
 def somemethod():
     pass